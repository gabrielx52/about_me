'use strict';

var varp = prompt('hello').toLowerCase();
alert(varp);

var varp = prompt().toLowerCase();

var varp = prompt().toLowerCase();

var varp = prompt().toLowerCase();

var varp = prompt().toLowerCase();
